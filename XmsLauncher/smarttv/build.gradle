apply plugin: 'com.android.application'


android {
    signingConfigs {
        config {
            keyAlias 'Test'
            keyPassword '201092'
            storePassword '201092'
        }
    }
    compileSdkVersion project.ext.compileSdkVersion
    buildToolsVersion project.ext.buildToolsVersion
    defaultConfig {
        applicationId "xms.com.smarttv"
        minSdkVersion project.ext.minSdkVersion
        targetSdkVersion project.ext.targetSdkVersion
        versionCode 1
        versionName "0.6"
        signingConfig signingConfigs.config
        vectorDrawables.useSupportLibrary = true
    }
    buildTypes {
        release {
            minifyEnabled true
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
    productFlavors {
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile 'com.android.support:leanback-v17:' + project.ext.supportLibraryVersion
    compile project(':xmsproplayer')
    compile('io.socket:socket.io-client:1.0.0') {
        // excluding org.json which is provided by Android
        exclude group: 'org.json', module: 'json'
    }
    compile 'com.android.support.constraint:constraint-layout:' + project.ext.constraintlayout
    compile 'com.github.bumptech.glide:glide:' + project.ext.glide
    compile 'com.android.support.constraint:constraint-layout:1.0.2'
    annotationProcessor 'com.github.bumptech.glide:compiler:' + project.ext.glide
    compile 'com.android.support:recyclerview-v7:' + project.ext.supportLibraryVersion
    compile 'com.android.support:support-v4:' + project.ext.supportLibraryVersion
    compile 'com.android.support:support-vector-drawable:' + project.ext.supportLibraryVersion
    compile 'org.osmdroid:osmdroid-android:' + project.ext.osm
    compile 'io.realm:android-adapters:2.1.1'
    compile 'com.yarolegovich:discrete-scrollview:1.4.4'

}
